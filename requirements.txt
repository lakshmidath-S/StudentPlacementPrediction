# ============================================================
# Adaptive Placement Intelligence — runtime dependencies
#
# There is no version pin against a serialised artifact any more. Models are
# trained by the user in whatever environment the app is running, and each
# bundle records the library versions that built it, so a mismatch is reported
# at load time instead of being prevented by a pin here.
# ============================================================

# Core data & modelling
numpy>=2.0.0
pandas>=2.2.0
scikit-learn>=1.5.0
scipy>=1.11.0
joblib>=1.3.0

# Optional accelerator. The planner falls back to scikit-learn's histogram
# boosting when it is absent, so the app runs without it.
xgboost>=2.0.0

# UI
streamlit>=1.44.0
plotly>=5.18.0

# Reading uploaded spreadsheets
openpyxl>=3.1.0

# Plan validation — the contract between the LLM and the executor
pydantic>=2.6.0

# LLM providers are called over plain REST; no vendor SDK is required.
requests>=2.31.0

# Loads GEMINI_API_KEY / XAI_API_KEY from a .env file during local development
python-dotenv>=1.0.0

# Testing & linting
pytest>=8.0.0
ruff>=0.3.0
