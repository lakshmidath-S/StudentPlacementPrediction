# Local Streamlit secrets. Gitignored — never commit a real key.
# Streamlit Community Cloud has no environment variables, so keys go here
# (Settings -> Secrets) rather than in a .env file.

# GEMINI_API_KEY = "your-key-here"
# XAI_API_KEY = "your-key-here"
