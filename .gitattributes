# Normalise line endings for source and docs so diffs stay clean across
# Windows and Linux checkouts.
* text=auto eol=lf

# Binary artifacts — never touch, never diff. Trained models live under
# workspaces/ and are not committed, but a stray .joblib must not be mangled.
*.joblib binary
*.png binary
*.db binary
