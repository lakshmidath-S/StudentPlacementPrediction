# ============================================================
# CI dependencies.
#
# Deliberately no API key and no LLM provider: CI exercises the rule-based
# planner, which is the path a fresh clone and any key-less deployment take. If
# that path ever stops producing a trainable model, the build must go red.
#
# Streamlit is included because the app is smoke-tested headlessly through
# streamlit.testing.AppTest.
# ============================================================

numpy>=2.0.0
pandas>=2.2.0
scikit-learn>=1.5.0
scipy>=1.11.0
joblib>=1.3.0
xgboost>=2.0.0

streamlit>=1.44.0
plotly>=5.18.0
openpyxl>=3.1.0

pydantic>=2.6.0
requests>=2.31.0
python-dotenv>=1.0.0

pytest>=8.0.0
ruff>=0.3.0
pyright>=1.1.350
